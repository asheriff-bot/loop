# prompts.txt
#
# loop.pdf deliverable: every prompt / command given to an agent.
# The loop engine APPENDS to this file automatically whenever it renders a
# prompt (see loop_engine/prompt_loader.append_to_prompts_log).
#
# Manual agent chats should also be pasted below under a dated heading.

## Bootstrap (human → Cursor)

Analyze loop.pdf and build loop engineering code from scratch inspired by
https://github.com/baidu-baige/LoongFlow (Plan-Execute-Summary), meeting the
assignment requirements, with human-engineer reasoning comments.

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build a nontrivial interactive web app (base system), then extend it with a meaningful feature — using the Plan-Execute-Summary loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build a nontrivial interactive web app (base system), then extend it with a meaningful feature — using the Plan-Execute-Summary loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build a nontrivial interactive web app (base system), then extend it with a meaningful feature — using the Plan-Execute-Summary loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build a nontrivial interactive web app (base system), then extend it with a meaningful feature — using the Plan-Execute-Summary loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

## Retrieved experiential memory (most recent first)

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1544 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build a nontrivial interactive web app (base system), then extend it with a meaningful feature — using the Plan-Execute-Summary loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Human → Cursor Agent (2026-07-14) — product build request
Use the AI + loop code correctly to build a small gaming application with
proper backend, functional generated code, runnable on localhost on a port
so the game can be played correctly.

========================================================================
# Prompt captured at loop iteration
# REVIEW MODE — critique specs/plan before building

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: review
- Stage: review
- Iteration: 1

## Retrieved experiential memory (most recent first)

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1846 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1544 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

## Instructions
1. Review `specs/*` and `IMPLEMENTATION_PLAN.md` (if present).
2. Check for: ambiguities, missing acceptance criteria, unrealistic deps,
   underspecified edge cases, and anything disconnected from the stated goal.
3. Ask/answer clarifying questions in the docs themselves (update the specs).
4. Produce a short review note in `IMPLEMENTATION_PLAN.md` under `## Review notes`.
5. Do not implement product code.

When the review is complete and blockers are either resolved or explicitly listed,
output `<promise>DONE</promise>`.

========================================================================
# Prompt captured at loop iteration
# PLAN MODE — analysis only (do not implement)

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: plan
- Assignment stage: plan
- Iteration: 1

## Retrieved experiential memory (most recent first)

### Iteration 1 [SUCCESS] stage=review
- Plan: Mode=review; prompt length=1667 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1846 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1544 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md` (create the plan file if missing).
1. Study the existing codebase. Do **not** assume features are missing — search first.
2. Compare code vs specs. Look for TODOs, stubs, skipped tests, and inconsistencies.
3. Update `IMPLEMENTATION_PLAN.md` as a prioritized bullet list of remaining work.
4. If a required capability is truly absent, author/extend a spec under `specs/`.

## Hard constraints
- **Plan only. Do NOT implement code changes** (docs/plan updates are OK).
- Prefer consolidating shared utilities over copy-paste.
- Keep `AGENTS.md` operational (commands/ports/patterns). Progress notes belong in `IMPLEMENTATION_PLAN.md`.

## When finished
Summarize remaining work and confidence. Output `<promise>DONE</promise>` only if the plan is complete and implementation-ready (no further planning iterations needed).

========================================================================
# AI (Cursor) response to PLAN prompt — step1_base/plan
Produced prioritized IMPLEMENTATION_PLAN.md backlog for Locksmith base:
engine, db, Flask API, static UI, tests, server script. No product code yet.
<promise>DONE</promise>

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

## Retrieved experiential memory (most recent first)

### Iteration 1 [SUCCESS] stage=plan
- Plan: Mode=plan; prompt length=2442 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=review
- Plan: Mode=review; prompt length=1667 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1846 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1544 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# AI (Cursor) BUILD execution — step1_base/build
Implemented Locksmith base per IMPLEMENTATION_PLAN:
- game/logic.py, db.py, app.py, templates, static
- tests (logic + API), script/server
- Verified: pytest green; server on :5055; play win via /api + UI index 200
## Lessons
- Mastermind duplicate handling needs exact-first multiset logic (unit-tested).
- Keep secrets server-side until game finishes; UI only renders public state.
- Flask + vanilla JS avoids a frontend build step for TA reproducibility.
<promise>DONE</promise>

========================================================================
# AI specify — step2_extension/specify
Authored specs/02-locksmith-extension.md: Hard mode (5×1-8, 12 guesses) +
GET /api/daily + mode-filtered leaderboards.

========================================================================
# Prompt captured at loop iteration
# PLAN MODE — analysis only (do not implement)

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: plan
- Assignment stage: plan
- Iteration: 1

## Retrieved experiential memory (most recent first)

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=3001 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=plan
- Plan: Mode=plan; prompt length=2442 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=review
- Plan: Mode=review; prompt length=1667 chars
- Execution: backend=echo; elapsed=0.00s; returncode=0; output_chars=285
- Reflection: Completion promise detected=False. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1846 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

### Iteration 1 [SUCCESS] stage=build
- Plan: Mode=build; prompt length=1544 chars
- Execution: backend=dry_run; elapsed=0.00s; returncode=0; output_chars=1360
- Reflection: Completion promise detected=True. Treating cycle as success.
- Lessons:
  - Completed a cycle; review IMPLEMENTATION_PLAN.md before the next iteration to avoid re-planning finished work.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md` (create the plan file if missing).
1. Study the existing codebase. Do **not** assume features are missing — search first.
2. Compare code vs specs. Look for TODOs, stubs, skipped tests, and inconsistencies.
3. Update `IMPLEMENTATION_PLAN.md` as a prioritized bullet list of remaining work.
4. If a required capability is truly absent, author/extend a spec under `specs/`.

## Hard constraints
- **Plan only. Do NOT implement code changes** (docs/plan updates are OK).
- Prefer consolidating shared utilities over copy-paste.
- Keep `AGENTS.md` operational (commands/ports/patterns). Progress notes belong in `IMPLEMENTATION_PLAN.md`.

## When finished
Summarize remaining work and confidence. Output `<promise>DONE</promise>` only if the plan is complete and implementation-ready (no further planning iterations needed).

========================================================================
# Prompt captured at loop iteration
# BUILD / EXECUTE MODE — implement one valuable increment

You are running inside a Plan–Execute–Summary loop engineering system.

## Goal
Build Locksmith: a Mastermind-style code-breaking browser game with a Flask + SQLite backend on localhost:5055 (base system), then extend it with a daily challenge and public leaderboard — using the PES loop for plan/build.

## Context
- Mode: build
- Assignment stage: build
- Iteration: 1

No prior experiential memory yet. This is the first iteration — explore carefully and record lessons in Summary.

## Instructions
0. Study `specs/*` and `IMPLEMENTATION_PLAN.md`.
1. Pick the **highest-priority unfinished** item. Search the codebase before coding.
2. Implement it fully — no stubs, placeholders, or "TODO later" exits.
3. Run the relevant tests for the unit you changed. Fix failures you cause (and any
   unrelated broken tests you touch).
4. Update `IMPLEMENTATION_PLAN.md` (mark done / add discoveries).
5. If you learn a new run command, update `AGENTS.md` briefly (operational only).
6. `git add` + `git commit` with a clear message describing *why*.

## Hard constraints
- Single sources of truth; avoid adapters/migrations unless the spec requires them.
- Keep progress out of `AGENTS.md`.
- Record durable learnings so the next loop iteration does not rediscover them.

## Lessons
After your work, write a short `## Lessons` section with 1–5 bullets about what
worked or failed (the Summary step stores these in experiential memory).

## When fully done
When **all** plan items for the current step are implemented and validated, output:
`<promise>DONE</promise>`

========================================================================
# AI plan/build — step2_extension
Plan: ModeConfig + /api/daily + hard UI/API + tests.
Build executed those items; pytest green.
## Lessons
- Mode parameters belong in one ModeConfig map so API + UI stay aligned.
- Monkeypatch generate_secret in tests for deterministic hard wins.
<promise>DONE</promise>
